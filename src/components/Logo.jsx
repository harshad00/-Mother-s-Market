import React from 'react';

function Logo({ className }) {
  return (
    <div className={`font-bold text-[#EF1717] ${className}`}>
      Mother&apos;s Market
    </div>
  );
}

export default Logo;

